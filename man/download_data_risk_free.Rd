% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_data_rf.R
\name{download_data_risk_free}
\alias{download_data_risk_free}
\title{Download Risk-Free Rate Data}
\usage{
download_data_risk_free(
  start_date = NULL,
  end_date = NULL,
  frequency = "monthly"
)
}
\arguments{
\item{start_date}{Optional. A character string or Date object in
"YYYY-MM-DD" format specifying the start date for the data. If
not provided, the full dataset is returned.}

\item{end_date}{Optional. A character string or Date object in
"YYYY-MM-DD" format specifying the end date for the data. If not
provided, the full dataset is returned.}

\item{frequency}{A character string, either \code{"monthly"} (default)
or \code{"daily"}, specifying the frequency of the returned data.}
}
\value{
A tibble with two columns:
\describe{
\item{date}{The date of the observation.}
\item{risk_free}{The risk-free rate for the period.}
}
}
\description{
Downloads and processes risk-free rate data from FRED. For monthly
data, splices the 3-Month Treasury Bill Secondary Market Rate
(TB3MS, pre-2001) with the 4-Week Treasury Bill Secondary Market
Rate (DTB4WK, from 2001 onwards). For daily data, uses the 3-Month
Treasury Bill Secondary Market Rate (DTB3) forward-filled to cover
weekends and holidays.
}
\details{
For monthly rates, the function splices two FRED series at
2001-07-01:
\itemize{
\item \strong{Pre-2001}: TB3MS (3-month T-bill, monthly). The annualised
discount rate \code{d} is converted to a monthly holding-period return
via \code{(1 + d * 90/360 / (1 - d * 90/360))^(1/3) - 1}.
\item \strong{From 2001}: DTB4WK (4-week T-bill, daily). The last non-NA
observation per calendar month is taken and the annualised
discount rate \code{d} is converted via
\code{(1 + d * 28/360 / (1 - d * 28/360))^(365/28/12) - 1}.
}

For daily rates, DTB3 (3-month T-bill, daily) is forward-filled and
the annualised discount rate \code{d} is converted to a daily
holding-period return via
\code{(1 + d * 90/360 / (1 - d * 90/360))^(1/63) - 1},
where 63 is the approximate number of trading days per quarter.
}
\examples{
\donttest{
  download_data_risk_free("2020-01-01", "2020-12-31")
  download_data_risk_free(
    "2020-01-01", "2020-12-31", frequency = "daily"
  )
}
}
\seealso{
Other download functions: 
\code{\link{download_data}()},
\code{\link{download_data_constituents}()},
\code{\link{download_data_factors_ff}()},
\code{\link{download_data_factors_q}()},
\code{\link{download_data_fred}()},
\code{\link{download_data_macro_predictors}()},
\code{\link{download_data_osap}()},
\code{\link{download_data_stock_prices}()}
}
\concept{download functions}
