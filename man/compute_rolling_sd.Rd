% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_rolling_standard_deviation.R
\name{compute_rolling_sd}
\alias{compute_rolling_sd}
\title{Compute a rolling standard deviation by period}
\usage{
compute_rolling_sd(data, column, period = "quarter", periods = 4, min_obs = 2)
}
\arguments{
\item{data}{A data frame with a \code{date} column of class \code{Date}.}

\item{column}{A column name (unquoted) to compute the rolling standard deviation on.}

\item{period}{A string specifying the period for rolling windows.
Common options include \code{"quarter"}, \code{"month"}, or \code{"year"}.}

\item{periods}{Number of periods to include in the rolling window.}

\item{min_obs}{Minimum number of non-missing observations required to compute the statistic.}
}
\value{
A numeric vector of rolling standard deviations, aligned with \code{data$date}.
}
\description{
Calculates a rolling standard deviation of a specified column over a given
number of time periods (e.g., quarters, months, years). Windows with fewer
than \code{min_obs} non-missing observations return \code{NA_real_}.
}
\examples{
library(dplyr)

df <- tibble(
  date = seq.Date(from = as.Date("2020-01-01"), by = "month", length.out = 24),
  value = rnorm(24)
)

df |> mutate(rolling_sd = compute_rolling_sd(df, value, period = "month", periods = 4, min_obs = 2))
}
