% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_data_stambaugh_yuan.R
\name{download_data_stambaugh_yuan}
\alias{download_data_stambaugh_yuan}
\title{Download and Process Stambaugh-Yuan Mispricing Factors}
\usage{
download_data_stambaugh_yuan(
  dataset = "monthly",
  start_date = NULL,
  end_date = NULL,
  url = "https://finance.wharton.upenn.edu/~stambaug/"
)
}
\arguments{
\item{dataset}{The data frequency to download, either \code{"monthly"} (the
default) or \code{"daily"}.}

\item{start_date}{Optional. A character string or Date object in
"YYYY-MM-DD" format specifying the start date for the data. If not
provided, the full dataset is returned.}

\item{end_date}{Optional. A character string or Date object in "YYYY-MM-DD"
format specifying the end date for the data. If not provided, the full
dataset is returned.}

\item{url}{The base URL from which to download the dataset files. The file
name (\code{M4.csv} or \code{M4d.csv}) is appended based on \code{dataset}.}
}
\value{
A tibble with the columns \code{date} (aligned to the beginning of the
month for monthly data), \code{mkt_excess} (the market excess return), \code{smb}
(size), \code{mgmt} (the management mispricing factor), \code{perf} (the performance
mispricing factor), and \code{risk_free} (the risk-free rate). All returns are
plain numeric (decimal) values, filtered by the specified date range if
\code{start_date} and \code{end_date} are provided.
}
\description{
Downloads and processes the mispricing factor data of Stambaugh and Yuan
(2017) from
\href{https://finance.wharton.upenn.edu/~stambaug/}{Stambaugh's data library}.
The four-factor model (M4) combines the market and size factors with two
mispricing factors, \code{mgmt} (management) and \code{perf} (performance). The
function downloads the requested frequency, aligns the date, renames the
columns to the package conventions, and optionally filters the data based on
a provided date range.
}
\details{
Returns are already expressed as plain numeric (decimal) values in the
source data, so no rescaling is applied. The source files currently end in
December 2016; a requested date range that lies entirely outside the
available data emits a warning and returns an empty tibble.
}
\examples{
\donttest{
  download_data_stambaugh_yuan(
    start_date = "2015-01-01", end_date = "2016-12-31"
  )
  download_data_stambaugh_yuan(
    dataset = "daily", start_date = "2016-01-01", end_date = "2016-12-31"
  )
}
}
\references{
Stambaugh, R. F., & Yuan, Y. (2017). Mispricing factors. \emph{Review of
Financial Studies}, 30(4), 1270-1315. \doi{10.1093/rfs/hhw107}
}
\seealso{
Other download functions: 
\code{\link{download_data}()},
\code{\link{download_data_constituents}()},
\code{\link{download_data_factors_ff}()},
\code{\link{download_data_factors_q}()},
\code{\link{download_data_fred}()},
\code{\link{download_data_huggingface}()},
\code{\link{download_data_jkp}()},
\code{\link{download_data_macro_predictors}()},
\code{\link{download_data_osap}()},
\code{\link{download_data_pastor_stambaugh}()},
\code{\link{download_data_risk_free}()},
\code{\link{download_data_stock_prices}()},
\code{\link{download_factor_library_grid}()},
\code{\link{download_factor_library_ids}()}
}
\concept{download functions}
