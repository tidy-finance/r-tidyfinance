% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_data_pastor_stambaugh.R
\name{download_data_pastor_stambaugh}
\alias{download_data_pastor_stambaugh}
\title{Download and Process Pastor-Stambaugh Liquidity Factors}
\usage{
download_data_pastor_stambaugh(
  start_date = NULL,
  end_date = NULL,
  url = paste0("https://faculty.chicagobooth.edu/-/media/faculty/lubos-pastor/data/",
    "liq_data_1962_2025.txt")
)
}
\arguments{
\item{start_date}{Optional. A character string or Date object in
"YYYY-MM-DD" format specifying the start date for the data. If not
provided, the full dataset is returned.}

\item{end_date}{Optional. A character string or Date object in "YYYY-MM-DD"
format specifying the end date for the data. If not provided, the full
dataset is returned.}

\item{url}{A character string with the URL of the liquidity data file.
Because the file name embeds the last year of data, the default points to
the most recent file known at release time; override it when a newer file
becomes available.}
}
\value{
A tibble with the columns \code{date} (aligned to the beginning of the
month), \code{agg_liq} (levels of aggregate liquidity), \code{innov_liq}
(innovations in aggregate liquidity, the non-traded liquidity factor), and
\code{traded_liq} (the traded liquidity factor LIQ_V), filtered by the
specified date range if \code{start_date} and \code{end_date} are provided.
}
\description{
Downloads and processes the liquidity factor data of Pastor and Stambaugh
(2003) from
\href{https://faculty.chicagobooth.edu/lubos-pastor/data}{Pastor's data library}.
The source is a whitespace-delimited text file whose header lines start with
a percent sign. The function reads the three liquidity series, aligns the
monthly date to the beginning of the month, and optionally filters the data
based on a provided date range.
}
\details{
The series are already expressed as plain numeric (decimal) values in the
source data, so no rescaling is applied. The traded liquidity factor is only
available from 1968 onward; earlier observations are coded as \code{-99} in the
source file and are returned as \code{NA}.
}
\examples{
\donttest{
  pastor_stambaugh <- download_data_pastor_stambaugh(
    start_date = "2020-01-01", end_date = "2020-12-31"
  )
}
}
\references{
Pastor, L., & Stambaugh, R. F. (2003). Liquidity risk and expected stock
returns. \emph{Journal of Political Economy}, 111(3), 642-685.
\doi{10.1086/374184}
}
\seealso{
Other download functions: 
\code{\link{download_data}()},
\code{\link{download_data_constituents}()},
\code{\link{download_data_factors_ff}()},
\code{\link{download_data_factors_q}()},
\code{\link{download_data_fred}()},
\code{\link{download_data_huggingface}()},
\code{\link{download_data_jkp}()},
\code{\link{download_data_macro_predictors}()},
\code{\link{download_data_osap}()},
\code{\link{download_data_risk_free}()},
\code{\link{download_data_stambaugh_yuan}()},
\code{\link{download_data_stock_prices}()},
\code{\link{download_factor_library_grid}()},
\code{\link{download_factor_library_ids}()}
}
\concept{download functions}
