% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_data_huggingface.R
\name{download_data_huggingface}
\alias{download_data_huggingface}
\title{Download data from a Hugging Face dataset}
\usage{
download_data_huggingface(
  dataset = NULL,
  start_date = NULL,
  end_date = NULL,
  type = deprecated(),
  ...
)
}
\arguments{
\item{dataset}{Character(1). The dataset to download. Supported values are
\code{"high_frequency_sp500"}, \code{"factor_library"}, and \code{"factor_library_grid"}.}

\item{start_date}{Date or character. Start date (inclusive) in
\code{"YYYY-MM-DD"} format. Used for \code{"high_frequency_sp500"} and
\code{"factor_library"}. When omitted for \code{"factor_library"}, the full return
history is returned; \code{"high_frequency_sp500"} falls back to a built-in
sample window.}

\item{end_date}{Date or character. End date (inclusive) in \code{"YYYY-MM-DD"}
format. See \code{start_date}.}

\item{type}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Use \code{dataset} instead.}

\item{...}{For \code{dataset = "factor_library"}: either named arguments used
to filter the portfolio grid, or \verb{ids = <vector>} to bypass the grid
filter and download specific portfolios directly via
\code{\link[=download_factor_library_ids]{download_factor_library_ids()}}. Filter arguments take the form
\code{column = value}, where \code{value} may be a vector to match multiple
levels. Optionally pass \code{fill_all = TRUE} to leave unspecified columns
unrestricted (default: \code{FALSE}, i.e. unspecified columns are fixed at
the defaults listed below). Passing \code{NULL} for any parameter removes
that filter entirely, returning all values for that column (e.g.,
\code{min_size_quantile = NULL} includes all size groups). Passing an
unrecognised column name raises an error listing the supported names.
\code{ids} cannot be combined with filter arguments. Ignored when
\code{dataset != "factor_library"}. See the Details section for supported
columns and their defaults.}
}
\value{
A tibble with the downloaded data. For \code{"high_frequency_sp500"},
contains 5-second aggregated orderbook snapshots filtered to the requested
date range. For \code{"factor_library"}, contains the columns \code{id}, \code{date}, and
\code{ret} joined with the full grid metadata for the matched portfolio IDs.
}
\description{
Downloads data from a supported Hugging Face dataset. For
\code{"high_frequency_sp500"}, parquet files are filtered by date range and
row-bound. For \code{"factor_library"}, portfolio characteristics are selected via
\code{filter_factor_library_grid()}, the matching return data is downloaded, and
the result is filtered to \code{start_date}/\code{end_date} when both are supplied.
For \code{"factor_library_grid"}, the grid itself is returned via
\code{\link[=download_factor_library_grid]{download_factor_library_grid()}}.
}
\details{
\strong{Note on \code{dataset = "factor_library"} defaults:} The defaults below reflect
one common portfolio construction choice, but may not suit every research
question. Always verify that the selected combination matches your intended
design.

Supported columns and their defaults for \code{...}:
\itemize{
\item \code{sorting_variable}: \strong{Required.} The firm characteristic used
to sort stocks into portfolios, named like the Open Source Asset
Pricing signals (e.g., \code{"size"} for market equity, \code{"bm"} for
book-to-market). See \code{\link[=download_factor_library_grid]{download_factor_library_grid()}} for all
values. No default is applied.
\item \code{min_size_quantile} (defaults to \code{0.2}): Fraction of the smallest
stocks (by market cap) excluded from the portfolio universe. \code{0.2}
drops the bottom 20\%.
\item \code{exclude_financials} (defaults to \code{FALSE}): Whether to drop
financial-sector stocks (SIC 6000-6799) from the universe.
\item \code{exclude_utilities} (default: \code{FALSE}): Whether to drop
utility-sector stocks (SIC 4900-4999) from the universe.
\item \code{exclude_negative_earnings} (defaults to \code{FALSE}): Whether to
drop firms with negative earnings before sorting.
\item \code{sorting_variable_lag} (defaults to \code{"6m"}): Lag applied to the
sorting variable before portfolio assignment: \code{"1m"} (the timing of
Open Source Asset Pricing), \code{"3m"}, \code{"6m"}, or \code{"ff"}
(Fama-French).
\item \code{rebalancing} (defaults to \code{"monthly"}): How frequently portfolios
are reformed: \code{"monthly"} or \code{"annual"}.
\item \code{n_portfolios_main} (defaults to \code{10}): Number of quantile groups
(e.g., \code{10} for decile portfolios).
\item \code{sorting_method} (defaults to \code{"univariate"}): Whether portfolios
are formed on a single sort (\code{"univariate"}) or on a double sort with
size as the second variable (\code{"bivariate-dependent"} or
\code{"bivariate-independent"}).
\item \code{n_portfolios_secondary} (defaults to \code{NULL}): Number of size
groups for the secondary sort.
Required when \code{sorting_method} is not \code{"univariate"}.
\item \code{breakpoints_exchanges} (defaults to: \code{"NYSE"}): Exchange(s) used
to compute breakpoints. \code{"NYSE"} uses only NYSE-listed stocks to
define quantile cutoffs (the conventional Fama-French approach).
\item \code{breakpoints_min_size_threshold} (defaults to \code{NA}): Minimum
size quantile of the stocks that set the main breakpoints (e.g.,
\code{0.2}). \code{NA} means no minimum-size screen is applied.
\item \code{weighting_scheme} (defaults to \code{"VW"}): Return weighting within
portfolios: \code{"VW"} for value-weighted, \code{"EW"} for equal-weighted, or
\code{"capped VW"} for value-weighted with capped weights.
}
}
\examples{
\dontrun{
  download_data_huggingface(
    "high_frequency_sp500", "2007-07-26", "2007-07-27"
  )
  download_data_huggingface(
    "factor_library",
    sorting_variable = "high52",
    rebalancing = "annual"
  )
  download_data_huggingface(
    "factor_library", sorting_variable = "assetgrowth", fill_all = TRUE
  )
  download_data_huggingface(
    "factor_library",
    sorting_variable = "size",
    start_date = "2000-01-01",
    end_date = "2020-12-31"
  )
  download_data_huggingface("factor_library", ids = c(1L, 2L, 3L))
}
}
\seealso{
Other download functions: 
\code{\link{download_data}()},
\code{\link{download_data_constituents}()},
\code{\link{download_data_factors_ff}()},
\code{\link{download_data_factors_q}()},
\code{\link{download_data_fred}()},
\code{\link{download_data_fred_md}()},
\code{\link{download_data_jkp}()},
\code{\link{download_data_macro_predictors}()},
\code{\link{download_data_osap}()},
\code{\link{download_data_pastor_stambaugh}()},
\code{\link{download_data_risk_free}()},
\code{\link{download_data_stambaugh_yuan}()},
\code{\link{download_data_stock_prices}()},
\code{\link{download_factor_library_grid}()},
\code{\link{download_factor_library_ids}()}
}
\concept{download functions}
